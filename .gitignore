# Rust
/target
rust_verify_test/target/
Cargo.lock
/liblib.rlib

# Emacs
*~
\#*\#
.\#*
.emacs.desktop
.emacs.desktop.lock
*.elc
auto-save-list
tramp
.\#*
\#*
*~
TAGS

# Old code
attic/
lib.long-type-*.txt
.verus-log/
.verus-solver-log/

# Logs — raw Z3 profile data only (GB sized)
logs/profile/*/*.profile
logs/profile/*/*.profile.gz
logs/profile/*/*.graph
logs/profile/*/*.graph.gz
.~lock.*#

# Generated harness crate roots from scripts/validate-standard.sh (regenerable).
scratch/harness-*/

# Executables rustc built from the scratch measurement programs beside them.
# Each is 4.5 MB and is regenerated by the `Run:` line in its own .rs file.
scratch/r217-debug-struct-finish
scratch/r217-derive-debug-plain-field
scratch/r217-unit-struct-debug
