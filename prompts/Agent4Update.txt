You are agent4 working in branch: ~/projects/APAS-VERUS-agent4/.
Your worktree has been updated.
Read senior-proof-engineer.mdc (from .cursor/rules).
Read senior-prose-engineer.mdc (from .cursor/rules).
REREAD ANY FILES BEFORE MODIFYING THEM as I modify files in emacs.

Read src/standards/* and apply them in your proposed work.
Fix trigger warnings as they occur using the rules.

We are working on plans/PBOGH.md — PROVE BIG OR GO HOME.
Read it. You are Agent 4: Tables + Priority Queues.

Current state: 3670 verified, 456 holes, 25 clean chapters.

Your chapters: Chap38, Chap43, Chap45, Chap47, Chap49, Chap51.
Total: 107 holes across 39 files.

Section 4.2 target files in your chapters (clean deps):
  Chap47/ParaHashTableStEph.rs (1 hole).

Other chapters have internal deps — they unblock each other as you clean them.

Priority: Chap45 → Chap47 → Chap43 → Chap49 → Chap51 → Chap38.

Also read plans/coarse-rwlock-migration-plan.md for the RwLock migration details.

Key rules:
- WARNING: Do NOT add accept() anywhere except the patterns defined in the standards
  (lock-boundary, eq/clone). Misplaced accepts defeat the proof. When in doubt, leave
  it as assume and flag it for review.
- NEVER write `requires true`. Per spec_wf_standard.rs: every &self method requires
  self.spec_<module>_wf(). Every &mut self method requires old(self).spec_<module>_wf().
  Every Self output ensures out.spec_<module>_wf(). Read the standard.
- Run scripts/validate.sh after changes. Show full output.
- Run scripts/holes.sh on your chapters to verify hole counts before and after.

REPORTING: Before committing, write plans/AGENT4.md with:
  1. What you changed and why.
  2. Holes closed (file, hole type, how resolved).
  3. Blockers — anything you could not fix and why.
  4. Verified count, hole count for your chapters.
This file is how the merge coordinator tracks your progress.

Propose a plan of work.
